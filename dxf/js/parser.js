/**
 * parser.js
 *
 * TODO: 클로드 챗에서 생성한 parser.js 내용을 이 파일에 붙여넣으세요.
 *
 * 이 파일이 제공해야 하는 함수:
 *   - parseDXF(text)               → { layers: { [layerName]: ring[][] } }
 *   - detectAlignLayer(data)       → { layer: string|null, type: string }
 *   - detectBorderLayer(data)      → string|null
 *   - getLandUseLayers(data, excl) → string[]
 *   - getBorderBBox(data, layer)   → { minX, minY, maxX, maxY }|null
 */

// ↓ 아래에 붙여넣기
